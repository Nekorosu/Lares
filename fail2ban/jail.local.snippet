[homeshare]
enabled = true
filter = homeshare
port = http,https
logpath = /var/log/homeshare/security.log
backend = polling
findtime = 15m
maxretry = 10
bantime = 1h
# Local brute force is still limited by the application's persistent locks.
ignoreip = 127.0.0.1/8 ::1 192.168.32.0/24
