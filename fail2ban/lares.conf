[Definition]
# fail2ban removes the RFC3339 timestamp before matching the event and address.
failregex = ^\s*\[(?:admin_login_failed|admin_totp_failed|invite_failed|rate_limited)\] ip=<HOST> ".*"\s*$
ignoreregex =
datepattern = ^%%Y-%%m-%%dT%%H:%%M:%%S%%z
